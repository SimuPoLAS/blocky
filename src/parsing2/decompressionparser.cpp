#include "decompressionparser.hpp"

DecompressionParser::DecompressionParser() { }

int DecompressionParser::fill_buffer(char* buffer, int bufferSize, std::vector<CompressedSection> &sections) {
    return 0;
}
